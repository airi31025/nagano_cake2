<%= render 'public/homes/header' %>

<h1>ショッピングカート</h1>
<%= button_to"カートを空にする",public_cart_items_alldelete_path %>
<table>
  <thead>
    <th>商品名</th>
    <th>単価（税込）</th>
    <th>数量</th>
    <th>小計</th>
    <th></th>
   </thead>
   <tbody>
     <% @cart_items.each do |cart_item| %>
       <tr>
         <td><%= cart_item.item_id %></td>
         <td><%= cart_item.item.with_tax_price %></td>
         <td><%= cart_item.amount %></td>
         <td><%= cart_item.subtotal %></td>
         <td><%= link_to"削除する",public_cart_item_path(cart_item.id) %></td>
         <% @合計金額 += cart_item.subtotal %>
       </tr>
       <% end %>
   </tbody>
</table>

<%= link_to"買い物を続ける",public_homes_top_path %>
<%= link_to"情報入力に進む",new_public_order_path %>

