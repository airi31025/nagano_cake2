<%= render 'header' %>

<h1>商品一覧</h1>
<%= @items.each do |item| %>
<div>
  <%= link_to public_item_path(public_item.id) do %>
  <%= image_tag item.image, size: "100x200" %>
  <% end %>
  <p><%= item.name %></p>
  <p><%= item.price %></p>
  </div>
<% end %>
<%= paginate items %>